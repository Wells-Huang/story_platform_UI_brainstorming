# 使用者故事與驗收標準 (User Stories & AC)

## 核心名詞定義
- **使用者 (Role)**：分為訪客 (未登入)、會員 (已登入)、管理者 (Admin)。
- **課程 / 套組 (Course / Package)**：同一件商品的不同稱呼。一個課程對應一個可販售套組，課程進行中可持續新增多個課程音檔。
- **課程音檔 (Audio)**：隸屬於某一課程 / 套組的單一音訊內容。
- **課程公開狀態**：分為「公開上架」與「下架停售」。
- **公開上架**：可被訪客瀏覽並可購買。
- **下架停售**：新用戶不可購買，但既有已取得有效權限的會員仍可看到並播放其原本有權限的內容。
- **課程進度狀態**：分為「開放報名」與「進行中」，僅作前台標示與篩選。
- **音檔狀態**：分為 `draft`、`published`、`hidden`。
- **draft 音檔**：僅管理者可見；管理者可於後台預覽，但不提供會員查看或播放。
- **published 音檔**：顯示於公開課程大綱中，具有效權限的會員可播放。
- **hidden 音檔**：不顯示於公開課程大綱中，但具有效課程權限的會員仍可見且可播放。MVP 不區分新舊會員，只要會員目前對該課程具有有效權限，即可看到並播放。
- **驗證狀態**：分為「未驗證」與「已驗證」。
- **帳號狀態**：分為 `active`、`restricted`、`disabled`。其中 `restricted` 僅代表「未完成驗證」，`disabled` 代表管理員停用。
- **檔案處理狀態**：課程音檔除內容狀態外，另有檔案處理狀態，分為 `uploading`、`processing`、`ready`、`failed`。MVP 主要上傳 MP3 檔案，不要求複雜轉檔流程，但仍需完成基本檔案檢查與 metadata 讀取後才可進入 `ready`。
- **課程級授權**：MVP 採用課程級授權。會員取得的是某一課程 / 套組的播放權限，不建立音檔級會員授權。會員可播放某音檔，需同時符合帳號為 `active`、對該音檔所屬課程具有至少一個 `active` 權限來源、音檔狀態為 `published` 或 `hidden`、且檔案處理狀態為 `ready`。

## MVP 優先級建議
- **Must have**：註冊登入、Email 驗證、課程瀏覽、購買與付款、訂單紀錄、已購內容播放、後台課程管理、手動開通、會員查詢。
- **Should have**：忘記密碼、重寄驗證信、退款後權限回收、操作紀錄、播放數據查詢與匯出。
- **Could have**：更細的營運篩選條件、異常登入封鎖規則優化、客服表單整合。
- **Won't have (MVP)**：自助退款、進階風控、自動客服流程。

## Grill 後確認的全域工程決策
- **權限模型**：MVP 採課程級授權，不做音檔級授權。
- **`hidden` 音檔**：不區分新舊會員；只要會員目前對課程有有效權限，就能在「我的課程」看到並播放。
- **付款與授權**：付款成功與權限開通是兩個步驟。付款成功但授權建立失敗時，不得把訂單回滾為付款失敗，應標記為授權待處理或建立異常紀錄供管理者重試。
- **付款通知異常**：付款通知若簽章、金額、訂單狀態、交易編號或其他驗證條件不一致，一律不得自動開通權限，只記錄事件並標記為需人工處理。
- **音檔保護**：音檔原始檔不得公開存取；前端只能以 `audio_id` 呼叫後端 `play-url` API，由後端重新檢查權限後簽發短效 URL。
- **錯誤音檔修正**：已發布或曾開放播放的音檔，不直接刪除整筆音檔資料；若上架錯誤，應以「更換音檔檔案」處理，保留原 `audio_id` 與操作紀錄。
- **高風險操作**：退款、停用會員、升級管理者、撤銷手動開通、更換已發布音檔檔案等操作，皆需二次確認、原因必填，並寫入 append-only 操作紀錄。

---

## 訪客 (未登入)

### 1. 註冊與驗證
**Story**: 身為一名訪客，我想使用 Email 或 Google 建立會員帳號，以便能購買課程並使用系統功能。

**AC (驗收標準)**:
- 系統支援「Email + 密碼」與「Google 登入」兩種註冊方式。
- 使用 Email 註冊時，Email 為必填且不得與既有會員重複；若重複，系統顯示「此 Email 已註冊」。
- 使用 Email 註冊時，密碼至少 8 碼。
- 使用 Email 註冊成功後，系統須發送驗證信；並建立「驗證狀態 = 未驗證、帳號狀態 = restricted（未完成驗證）」的帳號。
- 使用 Email 註冊建立的未驗證帳號，在完成驗證前不得視為啟用帳號，不可建立正式登入 session，不可購買課程，不可播放音檔。
- 未驗證帳號嘗試登入時，系統顯示「請先完成 Email 驗證」，並提供重寄驗證信入口。
- 驗證信連結須有有效期限；若連結失效，使用者可重新寄送驗證信。
- Email 驗證完成後，系統應將帳號更新為「驗證狀態 = 已驗證、帳號狀態 = active」。
- 以 Google OAuth 首次建立帳號時，系統僅可使用 Google 回傳且標示為已驗證的 Email 建立帳號，並直接建立為「驗證狀態 = 已驗證、帳號狀態 = active」。
- 若 Google 回傳 Email 未標示為已驗證，系統不得自動建立 `active` 帳號，也不得自動綁定既有會員。
- 若 Google 回傳且已驗證的 Email 與既有會員的正式登入 Email 相同，系統應自動綁定至既有帳號，不得建立重複帳號。
- Google 自動綁定僅應以目前已生效的登入 Email 為依據；待驗證中的新 Email 不得參與 Google 自動綁定。
- 若既有帳號原本為「未驗證 + restricted（未完成驗證）」的 Email 帳號，且使用者以同一 Email 的 Google 成功登入，系統應將帳號更新為「已驗證 + active」。
- 若既有帳號原本為 `disabled`，系統不得因 Email 驗證完成或 Google 登入而自動解除停用狀態。

### 2. 登入與帳號恢復
**Story**: 身為一名訪客，我想登入或找回帳號存取權，以便使用已購買的課程內容。

**AC (驗收標準)**:
- 輸入正確的 Email 與密碼後可成功登入會員帳號。
- 透過 Google 授權成功後可登入對應會員帳號。
- 僅「驗證狀態 = 已驗證」且「帳號狀態 = active」的帳號可正常登入並取得會員 session。
- 若帳號狀態為 `restricted`，系統不得建立會員 session，並應導向驗證提示與重寄驗證信流程。
- 若帳號狀態為 `disabled`，系統不得允許登入。
- 若帳號或密碼錯誤，系統顯示明確錯誤訊息，且不透露是哪一項資料錯誤。
- 提供忘記密碼功能，使用者可透過 Email 取得重設密碼連結。
- 重設密碼連結須有有效期限，過期後需重新申請。
- 會員重設密碼成功後，可立即使用新密碼登入，舊密碼失效。

### 3. 瀏覽課程與課程詳情
**Story**: 身為一名訪客，我想瀏覽目前公開上架的課程與價格，以便決定是否購買。

**AC (驗收標準)**:
- 未登入狀態下，訪客可查看所有「公開上架」的課程列表。
- 課程資料應至少包含課程公開狀態與課程進度狀態。
- 課程列表與詳情頁至少顯示課程名稱、售價、課程簡介、公開音檔內容大綱與課程進度狀態。
- 系統提供「進行中」或「開放報名」等課程進度狀態標示或篩選，幫助訪客快速找到可購買課程。
- 已下架停售的課程不得出現在公開可購買列表與公開課程詳情頁。
- 公開課程大綱僅顯示 `published` 音檔，不顯示 `draft` 與 `hidden` 音檔。

---

## 會員 (已登入)

### 4. 個人資料管理
**Story**: 身為一名會員，我想查看並修改我的個人資料與登入方式，以確保帳號安全與聯絡資訊正確。

**AC (驗收標準)**:
- 會員可以查看及編輯姓名、聯絡電話。
- 會員可以變更 Email；變更後需重新完成 Email 驗證，驗證完成前以新 Email 不得登入。在新 Email 驗證完成之前，舊 Email 仍保持有效登入。
- 會員申請變更 Email 時，系統必須檢查新 Email 是否已被其他會員作為正式登入 Email 或待驗證 Email 使用。
- 若新 Email 已被使用或正在等待其他會員驗證，系統不得送出變更驗證信，並應顯示「此 Email 已被使用」。
- 會員送出 Email 變更後，在新 Email 驗證完成前，新 Email 僅為待驗證狀態，不得作為登入 Email，也不得參與 Google 自動綁定或找回密碼流程。
- Google 自動綁定僅應以目前已生效的登入 Email 為依據，不得以待驗證中的新 Email 作為綁定目標。
- 新 Email 驗證完成後，新 Email 應成為正式登入 Email；舊 Email 不可再用於 Email + 密碼登入。
- 會員可以修改密碼；修改時需輸入舊密碼與兩次新密碼確認。
- 以 Google 建立帳號的會員，若尚未設定密碼，系統應允許其補設密碼，且補設後仍登入同一帳號。

### 5. 購買課程與查看訂單紀錄
**Story**: 身為一名會員，我想購買課程並查看我的訂單紀錄，以確保我能取得對應音檔的播放權限。

**AC (驗收標準)**:
- 結帳流程串接第三方金流 (如綠界科技 ECPay)，至少支援「信用卡」與「虛擬帳號 / ATM 轉帳」。
- 若會員對同一課程已存在任何 `active` 權限來源，系統不得再次建立新訂單，並應提示已擁有課程權限。
- 若會員對同一課程已存在待付款訂單，系統不得再次建立第二筆待付款訂單，並應引導會員返回原訂單。
- 若會員對同一課程已無任何 `active` 權限來源，且不存在待付款訂單，系統可允許再次購買並建立新訂單。
- 建立訂單後，系統應記錄訂單編號、會員、課程名稱、金額、付款方式與訂單狀態。
- 建立訂單時，系統應保存課程資訊快照，至少包含 `course_id`、`course_name_snapshot`、`price_snapshot`、`order_amount`、`payment_method` 與 `created_at`。
- 後續課程名稱或售價異動，不得影響既有訂單的金額與課程名稱紀錄。
- 訂單狀態流轉僅允許：待付款 -> 付款成功、待付款 -> 付款失敗、待付款 -> 已取消、待付款 -> 已逾期、付款成功 -> 已退款。
- 訂單一旦進入付款成功，不得再被更新為付款失敗、已取消或已逾期。
- 訂單一旦進入已退款，不得再被更新回付款成功。
- 信用卡付款成功且系統接收到有效付款完成通知後，應建立或啟用該訂單來源的課程級權限，使該課程內所有目前可供有效權限會員播放的音檔自動解鎖。
- ATM / 虛擬帳號訂單在付款完成前狀態為「待付款」；未完成付款不得解鎖課程內容。
- 若付款失敗、取消或逾期，訂單狀態需正確更新，且不得授權播放權限。
- 第三方金流付款通知必須先完成來源與內容驗證，例如簽章 / CheckMacValue、訂單編號、付款金額、交易編號與付款狀態。付款金額應與訂單成立時保存的 `order_amount` 比對，不得以目前課程售價作為比對依據。
- 若付款通知驗證失敗、訂單不存在、付款金額不一致、交易編號重複、通知內容與目前訂單狀態衝突，或訂單狀態不允許轉為付款成功，系統不得自動開通權限，僅記錄付款事件並標記為需人工處理。
- 若同一筆訂單重複收到付款完成通知，系統不得重複建立權限或重複記帳。
- 若相同付款交易通知重送，系統不得新增第二筆成功付款紀錄，也不得新增第二筆 `active` 權限。
- 若訂單已為付款成功後再收到成功通知，系統僅記錄通知，不得重複授權。
- 若訂單已為已退款後再收到舊的成功通知，系統僅記錄通知，不得恢復訂單狀態或恢復權限。
- 若 ATM / 虛擬帳號訂單已逾期後才收到付款成功通知，系統僅記錄付款事件，不得自動授權，並交由人工處理。
- 付款完成通知處理應具備冪等性。系統收到有效付款成功通知後，應依序記錄付款事件、更新訂單狀態為付款成功、建立或啟用該訂單來源的課程權限，並記錄權限建立結果。
- 若付款成功但權限建立失敗，系統不得回滾付款成功狀態，應將訂單標記為授權待處理或建立異常紀錄，並允許管理者重新觸發權限開通。重新觸發權限開通不得建立重複 `active` 權限。
- 個人中心需顯示付款、購買與授權紀錄，至少列出課程名稱、紀錄來源（訂單 / 管理員手動開通）、付款方式或來源類型、金額（若有）、目前狀態，以及建立 / 付款 / 開通時間。

### 6. 已購課程與音檔播放
**Story**: 身為一名會員，我想播放我有權限的課程音檔，並操作播放進度，以利自由學習。

**AC (驗收標準)**:
- 在「我的課程」中，會員能看到所有目前具有有效播放權限的課程與音檔清單。
- 會員在「我的課程」中可看到 `published` 與 `hidden` 音檔；不得看到 `draft` 音檔。
- 會員只能播放自己具備有效課程權限的音檔；未購買或權限失效的音檔不得播放。
- 會員可播放某音檔需同時符合：會員帳號狀態為 `active`、會員對該音檔所屬課程具有至少一個 `active` 權限來源、音檔狀態為 `published` 或 `hidden`、且檔案處理狀態為 `ready`。
- 播放器取得的播放 URL 應為短效 presigned URL，TTL 固定為 `5400` 秒（90 分鐘）。
- 音檔原始檔案不得以公開 URL 直接存取；前端不得保存或推導永久儲存路徑。
- 前端只能以 `audio_id` 呼叫後端 `play-url` API，不得傳入任意 storage key、檔案路徑或原始檔 URL。
- 後端簽發 `play-url` 前，必須重新檢查會員帳號狀態、課程權限來源、音檔所屬課程、音檔內容狀態與檔案處理狀態。
- 播放成功的定義為：成功取得 `play-url`、播放器成功載入音檔、點擊播放後音檔可開始播放且播放時間持續往前推進。
- 點擊音檔可開啟網頁播放器，至少支援播放、暫停、快轉、倒轉與拖曳進度。
- 在權限有效且網路正常情況下，會員點擊播放後，播放器應於 `5` 秒內開始播放。
- 播放器畫面應顯示音檔總長度與目前播放進度。
- 使用者拖曳、快轉或倒轉後，播放器應跳至指定位置並於 `3` 秒內恢復播放。
- 若播放失敗原因為 URL 過期，且會員仍具有效權限，系統應自動重新取得新的 `play-url`，且不要求使用者重新登入。
- 若重新取得 `play-url` 時會員已無有效權限，系統不得恢復播放，並應顯示明確的無播放權限訊息。
- 若 `play-url` 取得失敗或播放器載入失敗，系統應顯示可理解錯誤訊息，並提供重試操作。
- 當課程新增 `published` 或 `hidden` 且檔案處理狀態為 `ready` 的音檔，且會員仍具有效課程權限時，該音檔應自動出現在其可播放清單中。
- 音檔若曾執行「更換音檔檔案」，會員端在音檔詳情或播放頁應顯示音檔最近更新時間，例如「音檔更新時間：YYYY-MM-DD HH:mm」。MVP 不提供站內通知、Email 通知或已聽會員重新通知機制。

### 7. 退款與客服聯繫
**Story**: 身為一名會員，我想在遇到課程問題或退款需求時能快速聯繫客服，以便取得協助。

**AC (驗收標準)**:
- 在播放頁與個人中心提供「聯繫客服」入口。
- 「聯繫客服」入口僅供會員送出請求，MVP 不提供站內即時客服功能。
- 點擊後可導向單一官方指定的客服管道，例如 Line、客服信箱或外部表單。
- 若客服管道連結未設定，前台不得顯示空連結，應改顯示預設提示文案。
- 退款申請由會員透過客服入口送出，後續由管理者人工處理；系統需保留對應訂單與會員資訊以供查詢。
- 當訂單狀態被標記為「已退款」後，會員應立即失去該訂單來源對應課程的播放權限。
- 當訂單狀態被標記為「已退款」後，系統不得再為該訂單來源簽發新的播放 URL。
- 若播放 URL 於退款前已簽發，該 URL 可能在剩餘有效期限內仍可使用，最長不得超過 `5400` 秒（90 分鐘）。
- 若會員因其他有效訂單或管理員手動開通仍持有同一課程權限，退款不得移除其其他合法權限來源。
- 若會員仍持有其他有效權限來源，系統應允許其以其他有效來源正常取得新的播放 URL。
- 已退款訂單需保留在訂單紀錄中，並明確標示為「已退款」。

---

## 管理 / 營運 (Admin)

### 8. 課程與音檔管理
**Story**: 身為一名管理者，我想建立課程、管理音檔與控制上架狀態，以便持續營運與販售內容。

**AC (驗收標準)**:
- 管理者可以建立課程，並設定課程名稱、售價、簡介、課程公開狀態與課程進度狀態。
- 管理者可以上傳音訊檔案至指定課程，且系統需顯示上傳進度。
- MVP 主要上傳 MP3 檔案，但系統仍應在上傳後執行基本檔案檢查與 metadata 讀取，例如檔案格式、檔案大小與音檔長度。
- 音檔檔案處理狀態包含 `uploading`、`processing`、`ready`、`failed`；只有檔案處理狀態為 `ready` 的音檔才可提供會員播放。
- 上傳音檔時，可填寫至少標題、排序、簡介與音檔狀態等基本欄位。
- 管理者可以調整課程內音檔順序，並新增或移除尚未發布且從未對會員開放的 `draft` 音檔。
- 若課程已有會員購買，該課程不得被實體刪除，只能下架停售。
- 已發布或曾開放給會員播放的音檔，不得直接刪除整筆音檔資料。若管理者因上架錯誤、檔案錯置、音訊內容錯誤或檔案損毀等原因需要修正，系統應提供「更換音檔檔案」功能。
- 更換音檔檔案時，系統應保留原本的音檔資料 ID、所屬課程、標題、排序與狀態；系統可更新實際音檔檔案、檔案大小、音檔長度、storage key、checksum 與檔案處理狀態。
- 更換音檔檔案時，管理者必須填寫更換原因，且系統必須寫入操作紀錄，包含操作者、操作時間、操作類型、目標音檔、舊檔案資訊、新檔案資訊與更換原因。
- 更換完成後，會員後續取得新的 `play-url` 時，應播放新檔案。更換前已簽發的短效播放 URL 可能在剩餘 TTL 內仍可播放舊檔案，最長不得超過 `5400` 秒，除非系統具備立即失效舊 URL 的能力。
- 音檔若曾執行「更換音檔檔案」，系統應記錄 `audio_file_updated_at`。
- `hidden` 音檔不得出現在公開課程大綱中。
- 管理者可於後台預覽 `draft`、`published` 與 `hidden` 音檔，但音檔檔案處理狀態必須為 `ready`。
- 管理者後台預覽不得視為會員播放權限，不得出現在會員端，也不得計入會員有效播放次數。
- `draft` 音檔僅管理者可見，不得提供會員查看或播放。
- 下架停售後，課程不得再被前台會員自行購買，但既有已購買會員仍可於「我的課程」中播放。
- 管理者仍可於後台對下架停售課程執行手動開通，用於客服補救、線下付款補登或特殊個案處理；此操作需二次確認、原因或備註必填，並寫入 append-only 操作紀錄。

### 9. 線下收款與手動開通權限
**Story**: 身為一名管理者，我想在會員透過線下匯款等方式付款後，手動開通課程權限給該會員，以確保其能順利上課。

**AC (驗收標準)**:
- 後台提供「手動開通課程」功能。
- 管理者可透過會員 Email 或帳號查詢並選定會員。
- 管理者可指定課程、填寫備註並確認開通。
- 若會員已透過管理員手動開通取得某課程 `active` 權限，系統應視為已擁有該課程權限，不得再次建立該課程的新購買訂單。
- 管理者可手動開通下架停售課程，但必須經二次確認，且原因或備註為必填。
- 若會員對同一課程已存在一筆 `active` 的手動開通權限，系統不得新增第二筆 `active` 手動開通。
- 開通成功後，會員的「我的課程」應立即顯示該課程與其可播放音檔。
- 系統需留下來源紀錄，標示為「管理員手動開通」並保留開通時間、操作者與備註。
- 手動開通與撤銷手動開通皆必須寫入操作紀錄，且至少包含操作者、操作時間、操作類型、目標對象、變更前狀態、變更後狀態與備註或原因。
- 後台應提供撤銷手動開通功能；撤銷時需填寫原因或備註，並保留操作者與時間紀錄。
- 撤銷手動開通後，僅移除該筆手動開通來源的權限；若會員仍有其他有效來源，課程權限應保留。

### 10. 會員與權限管理
**Story**: 身為一名管理者，我想查看會員資料、購買紀錄與權限狀態，以維護系統秩序並支援會員查單與爭議處理。

**AC (驗收標準)**:
- 後台可查看所有註冊會員列表，至少包含會員姓名、Email、註冊時間與帳號狀態。
- MVP 不設獨立客服角色；所有會員查詢、退款標記與爭議處理皆由管理者於後台執行。
- 後台至少支援以會員 Email、訂單編號或課程名稱查詢會員與訂單資料。
- 後台查看會員時，系統應可區分顯示驗證狀態，並清楚顯示 `restricted` 代表未完成驗證、`disabled` 代表管理員停用。
- 管理者可查看單一會員的基本資料、驗證狀態、帳號狀態、訂單紀錄、退款狀態、手動開通紀錄與目前有效課程權限，且可辨識各權限的來源。
- 系統應支援將普通會員升級為管理者，但此操作需有二次確認流程，原因必填，且保留操作者與時間紀錄。
- 系統可將特定會員標記為停用，並保留原因與操作紀錄。
- 會員帳號被標記為 `disabled` 後，不得再登入系統；既有會員 session 應失效，或於下一次需要授權的請求時被拒絕；不得取得新的 `play-url`。
- 若停用前已簽發短效播放 URL，該 URL 可能在剩餘有效期限內仍可使用，最長不得超過 `5400` 秒，除非系統具備立即失效舊 URL 的能力。
- 高風險操作包含但不限於：將訂單標記為已退款、停用會員、升級普通會員為管理者、撤銷手動開通權限、更換已發布或曾開放播放的音檔檔案。
- 執行高風險操作時，系統必須提供二次確認流程，且原因或備註為必填。操作完成後必須寫入操作紀錄。
- 每筆操作紀錄至少包含操作者、操作時間、操作類型、目標對象類型與 ID、變更前狀態、變更後狀態、原因或備註。
- 將訂單標記為已退款、停用會員、升級普通會員為管理者、撤銷手動開通權限、更換已發布或曾開放播放的音檔檔案時，原因或備註為必填。
- 系統應支援一個最初建立的 `bootstrap admin` 或系統初始管理者。
- 管理者不得停用自己的帳號。
- `bootstrap admin` 不得被停用、降權或刪除。
- 系統不得允許停用、降權或移除最後一個 `active` 管理者，以避免後台無人可管理。
- 若操作會導致系統不存在任何 `active` 管理者，系統應拒絕操作並顯示明確錯誤訊息。
- 操作紀錄採 append-only，MVP 不提供編輯或刪除功能。
- 管理者可在會員頁或訂單頁查看與該對象相關的操作紀錄。

### 11. 數據與營運管理
**Story**: 身為一名管理者，我想查看課程音檔的播放數據與會員購課明細，以優化營運策略並處理客服需求。

**AC (驗收標準)**:
- 管理者可在單一音檔後台查看累計播放次數。
- 一次有效播放定義為：同一會員在同一播放 session 中，對同一音檔連續播放超過 30 秒。
- 同一會員、同一音檔、同一播放 session 最多計一次有效播放。播放期間的暫停、繼續播放、快轉、倒轉與拖曳進度，不得造成重複計次。
- 重新整理頁面、重新開啟播放頁或重新取得新的播放 session，可視為新的播放 session；若再次連續播放超過 30 秒，可重新計為一次有效播放。
- 管理者可查閱特定會員的購課明細，包含線上付款、退款與手動開通紀錄。
- 管理者可匯出特定會員的購課明細，作為爭議處理依據。
- MVP 匯出範圍僅限單一會員購課明細，不提供全站批次報表匯出。
- 匯出格式固定為 `CSV UTF-8`，時間欄位固定以 `Asia/Taipei` 顯示。
- 匯出欄位至少包含會員姓名、會員 Email、課程名稱、訂單編號、訂單狀態、付款方式、金額、建立時間、付款時間、退款時間、權限來源與手動開通備註。


---

## 資料模型草案

> 本章節不是完整資料庫 schema，而是用來對齊 AI coding 與工程實作時的資料邊界。

### users
代表系統使用者，包含訪客註冊後的會員與管理者。

建議欄位：
- id
- name
- email
- pending_email
- email_verified_at
- password_hash
- google_account_id
- role: `member` / `admin`
- account_status: `active` / `restricted` / `disabled`
- is_bootstrap_admin
- created_at
- updated_at

### courses
代表可販售的課程 / 套組。

建議欄位：
- id
- title
- description
- price
- public_status: `public` / `unlisted`
- progress_status: `open_for_registration` / `in_progress`
- created_at
- updated_at

### audios
代表隸屬於某課程的單一音檔內容。

建議欄位：
- id
- course_id
- title
- description
- sort_order
- content_status: `draft` / `published` / `hidden`
- processing_status: `uploading` / `processing` / `ready` / `failed`
- storage_key
- file_size
- duration_seconds
- checksum
- audio_file_updated_at
- created_at
- updated_at

### orders
代表會員針對某課程建立的購買訂單。

建議欄位：
- id
- order_no
- user_id
- course_id
- course_name_snapshot
- price_snapshot
- order_amount
- payment_method
- status: `pending` / `paid` / `failed` / `cancelled` / `expired` / `refunded`
- authorization_status: `not_granted` / `granted` / `grant_pending` / `grant_failed`
- paid_at
- refunded_at
- created_at
- updated_at

### payment_events
代表第三方金流回傳的付款、失敗、逾期、重送或異常通知。

建議欄位：
- id
- order_id
- provider
- provider_transaction_id
- event_type
- amount
- raw_payload
- verification_status: `valid` / `invalid` / `conflict` / `manual_review_required`
- handled_at
- created_at

### course_access_grants
代表會員對某課程的有效或歷史權限來源。MVP 採課程級授權，不建立音檔級會員授權。

建議欄位：
- id
- user_id
- course_id
- source_type: `order` / `manual`
- source_id
- status: `active` / `revoked` / `refunded`
- granted_at
- revoked_at
- reason

### admin_audit_logs
代表管理者的 append-only 操作紀錄。

建議欄位：
- id
- actor_user_id
- action_type
- target_type
- target_id
- before_state
- after_state
- reason
- created_at

### play_sessions
代表會員端播放 session，用於有效播放次數計算與播放資料分析。

建議欄位：
- id
- user_id
- audio_id
- course_id
- started_at
- counted_as_valid_play_at
- total_played_seconds
- user_agent
- ip_hash


---

## 2026-05-13 追加 / 修訂：批次音檔上傳與後台篩選

### A. 管理員批次上傳課程音檔
**Story**: 身為管理員，我想在單一課程底下一次上傳多個音檔，讓課程音檔很多時不需要一筆一筆建立。

**AC**:
- 管理員可在課程詳情頁一次選擇多個音訊檔案上傳，單次最多 `25` 個。
- 批次上傳沿用既有 S3 direct upload 流程，並限制同時上傳數量，避免大量大檔案同時佔滿瀏覽器與網路資源。
- 每個音檔上傳成功後建立一筆音檔資料；部分失敗不阻擋其他成功項目。
- 批次結果需逐筆顯示完成、失敗、上傳中、進度與錯誤訊息；成功的音檔保留，失敗的檔案可重試。
- 音檔標題與簡介預設取自檔名，但不包含副檔名。
- 若檔名前綴是兩位數排序碼，系統自動帶入排序，並從標題/簡介移除排序碼：
  - `01_aaa.mp3` -> 排序 `1`，標題/簡介 `aaa`
  - `01-aaa.mp3` -> 排序 `1`，標題/簡介 `aaa`
  - `01aaa.mp3` -> 排序 `1`，標題/簡介 `aaa`
- 日期型檔名不得被誤判為排序碼，例如 `2024-02-06 aaa.mp3` 不應被當成排序 `20`。
- 若排序衝突，不阻擋上傳；系統依上傳順序微調成不重複排序。
- 系統需嘗試讀取 mp3 metadata，自動填入 `duration_seconds`；若讀取失敗，長度可保留空值，不阻擋上傳。
- 批次上傳建立的音檔預設狀態為 `published`。

### B. 後台內容管理篩選
**Story**: 身為管理員，我想在內容管理頁用篩選條件快速縮小課程與音檔列表，讓我能快速找到要編輯的課程或要處理的音檔。

**AC**:
- 內容管理頁的內容清單上方需提供與播放記錄頁一致的 GET 篩選表單。
- 篩選條件需反映在 URL query，重新整理或分享連結時仍保留條件。
- 需提供「清除篩選」回到未篩選狀態。
- 課程是管理員高頻操作入口，因此主要篩選條件應為「課程」下拉式選單，而不是自由文字關鍵字。
- 內容管理頁至少支援以下篩選：
  - 課程
  - 課程狀態
  - 公開狀態
  - 音檔狀態
- 篩選後需顯示符合課程數與符合音檔數。
- 篩選後課程卡片底下只顯示符合目前音檔狀態條件的音檔；若該課程沒有符合條件的音檔，需顯示清楚的空狀態。

### C. 會員權限最近紀錄篩選
**Story**: 身為管理員，我想在會員權限頁的最近紀錄中縮小檢視範圍，讓我能快速找到特定會員、課程或狀態的手動授權紀錄。

**AC**:
- 會員權限頁的最近紀錄上方需提供與播放記錄頁一致的 GET 篩選表單。
- 篩選條件需反映在 URL query，並提供「清除篩選」。
- 最近紀錄至少支援以下篩選：
  - 關鍵字：會員名稱、Email、課程名稱、課程 slug、備註、管理員名稱或 Email
  - 會員
  - 課程
  - 權限狀態
- 篩選後需顯示符合紀錄數。
- 若沒有符合條件的紀錄，需顯示清楚的空狀態。

### D. 會員管理列表篩選
**Story**: 身為管理員，我想在會員管理頁快速篩選會員列表，讓會員數增加後仍能有效找到目標會員。

**AC**:
- 會員管理頁的會員列表上方需提供與播放記錄頁一致的 GET 篩選表單。
- 篩選條件需反映在 URL query，並提供「清除篩選」。
- 會員列表至少支援以下篩選：
  - 關鍵字：會員姓名或 Email
  - 角色：會員 / 管理員
  - 帳號狀態：啟用 / 受限 / 停用
  - Email 驗證狀態：已驗證 / 未驗證
- 篩選後需顯示符合會員數。
- 若沒有符合條件的會員，需顯示清楚的空狀態。

### E. Dev-as-Prod 安全前提
**Story**: 因客戶可能暫時把 dev 環境當作正式環境使用，系統在 AWS dev runtime 應比照 production-like 防護，不應暴露本機測試用 debug 行為。

**AC**:
- `APP_ENV=dev` 應視為 production-like runtime。
- production-like runtime 必須設定 `AUTH_URL` 與 `SES_FROM_EMAIL`。
- production-like runtime 不得回傳 Email 驗證或忘記密碼的 debug link。
- 本機 `APP_ENV=local` / `test` 可保留 debug email fallback，方便 localhost QA。
